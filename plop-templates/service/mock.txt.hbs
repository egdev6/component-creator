  {
      "pattern": "/mock/{{camelName}}",
      "method": "{{http}}",
      "delay": 100,
      "handle": async (_req, res) => {
        const data = await import('./data/{{kebabName}}.json');
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify(data));
      }
  }